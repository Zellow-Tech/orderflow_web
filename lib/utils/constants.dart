import 'package:flutter/material.dart';

class ColorPalette {
  // The main blue color used in buttons and stuff
  final Color primaryBlue = const Color(0xff2238CC);

  // The blue used in clickable links in the UI
  final Color linkBlue = const Color(0xff1976D2);

  // The blue in id verifications button
  final Color accentBlue = const Color(0xffE0E4FE);

  // the color used in icons when inactive
  final Color inactiveIconGrey = const Color(0xff878484);

  // grey used in highlighted containers
  final Color containerGrey = const Color(0xffEDE8E8);

  // The color for secondary/not so important text
  final Color secondaryTextColor = const Color(0xff393939);

  // the tirtiary text color
  final Color tertiaryTextColor = const Color(0xff878484);

  // the green userd by github for greens
  final Color liveGreen = const Color(0xff7CFC00);

  // deletion res accent
  final Color deletionRed = const Color(0xffFF2511);

  // text grey
  final Color textGrey = const Color(0xff686868);

  // accent green
  final Color accentGreen = const Color(0xff6BC7A9);

  // touch purple
  final Color touchGreen = const Color(0xffe9fff3);

  // lime green
  final Color mainGreen = const Color(0xff06c871);

  // button icon green
  final Color buttonIconGreen = const Color(0xff00A878);

  // the final button icons bg red
  final Color buttonBgRed = const Color(0xffffe4e4);

  // the button secondary color
  final Color secondaryButtonOrange = const Color.fromRGBO(238, 142, 99, 100);

  // the secondary button color green
  final Color secondaryButtonGreen = const Color.fromRGBO(156, 222, 178, 100);

   // final moon raker purple
   final Color purpleLight = const Color(0xffDDD1F5);
}
