import 'package:flutter/material.dart';

class OFGColorPalette {
  //  PRIMARY BRAND COLORS
  /// Main brand color used for buttons, headers, and primary actions.
  final Color primaryBlue = const Color(0xff2238CC);

  /// Secondary blue used for clickable links and navigation items.
  final Color linkBlue = const Color(0xff1976D2);

  /// Light blue used for background highlights (e.g., ID verification).
  final Color highlightBlue = const Color(0xffE0E4FE);

  //  TEXT COLORS
  /// Primary text color for important UI elements.
  final Color primaryText = const Color(0xff393939);

  /// Secondary text color for subtitles, descriptions, etc.
  final Color secondaryText = const Color(0xff686868);

  /// Tertiary text color for less important details.
  final Color tertiaryText = const Color(0xff878484);

  //  ICON & UI ELEMENT COLORS
  /// Used for inactive icons and disabled UI elements.
  final Color inactiveIcon = const Color(0xff878484);

  /// Light grey for container backgrounds (e.g., cards, dividers).
  final Color containerGrey = const Color(0xffEDE8E8);

  //  STATUS COLORS
  /// Green used for success statuses (e.g., live indicators).
  final Color successGreen = const Color(0xff7CFC00);

  /// Red used for deletion warnings or critical alerts.
  final Color errorRed = const Color(0xffFF2511);

  /// Soft green for success-related highlights (e.g., completed status).
  final Color accentGreen = const Color(0xff6BC7A9);

  //  BUTTON COLORS
  /// Primary green for CTA (Call-To-Action) buttons.
  final Color ctaGreen = const Color(0xff06c871);

  /// Icon color used in green buttons.
  final Color buttonIconGreen = const Color(0xff00A878);

  /// Background color for red buttons (e.g., delete actions).
  final Color buttonBgRed = const Color(0xffffe4e4);

  /// Secondary button color (Orange variant).
  final Color buttonSecondaryOrange = const Color.fromRGBO(238, 142, 99, 100);

  /// Secondary button color (Green variant).
  final Color buttonSecondaryGreen = const Color.fromRGBO(156, 222, 178, 100);

  //  ACCENT & EXTRA COLORS
  /// Light purple used for special UI highlights.
  final Color accentPurple = const Color(0xffDDD1F5);

  /// Soft green for backgrounds and light themes.
  final Color backgroundGreen = const Color(0xffe9fff3);
}
